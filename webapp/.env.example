# ============================================
# RPC ENDPOINTS (In priority order)
# ============================================
# PRODUCTION NOTE: Use premium RPC providers for better reliability and performance
# Free tier public RPCs have rate limits and may be slower

# Primary: Helius (fastest, most reliable) - RECOMMENDED FOR PRODUCTION
NEXT_PUBLIC_HELIUS_RPC=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# Secondary: QuickNode (fallback) - RECOMMENDED FOR PRODUCTION
NEXT_PUBLIC_QUICKNODE_RPC=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_KEY

# Fallback: Public RPC (last resort - rate limited)
NEXT_PUBLIC_SOLANA_RPC_PRIMARY=https://api.mainnet-beta.solana.com

# Legacy RPC URL (for backwards compatibility)
# This is used if no other RPC is configured
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# ============================================
# API CONFIGURATION
# ============================================
# PRODUCTION NOTE: These endpoints are automatically configured with defaults
# Override only if you need to use custom endpoints

# Jupiter Quote API (v6) - For swap quotes
NEXT_PUBLIC_JUPITER_QUOTE_API=https://quote-api.jup.ag/v6

# Jupiter Price API (v6) - For token prices
NEXT_PUBLIC_JUPITER_PRICE_API=https://price.jup.ag/v6

# Jupiter Worker API - For airdrops
NEXT_PUBLIC_JUPITER_WORKER_API=https://worker.jup.ag

# Jupiter Token List API
NEXT_PUBLIC_JUPITER_TOKEN_LIST_API=https://token.jup.ag/all

# Legacy Jupiter API URL (for backwards compatibility)
NEXT_PUBLIC_JUPITER_API_URL=https://quote-api.jup.ag/v6

# WalletConnect Project ID (Required for wallet connections)
# Get yours at: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=YOUR_PROJECT_ID

# Pyth Network Configuration (for price feeds)
NEXT_PUBLIC_PYTH_HERMES_ENDPOINT=https://hermes.pyth.network
PYTH_HERMES_ENDPOINT=https://hermes.pyth.network

# Jito API (for MEV protection and airdrops)
NEXT_PUBLIC_JITO_API_URL=https://kek.jito.network/api/v1

# ============================================
# SERVER-SIDE ONLY (NOT NEXT_PUBLIC_)
# ============================================
# These variables are only accessible server-side
# DO NOT prefix with NEXT_PUBLIC_ to keep them secure

# Transaction Signing (Server-side only)
TRANSACTION_SIGNING_KEY=your_key_here

# Neynar API (Server-side only)
NEYNAR_API_KEY=your_key_here

# Database (Server-side only)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
