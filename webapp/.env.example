# ==============================================================================
# GXQ Studio Webapp - Environment Configuration
# ==============================================================================

# ------------------------------------------------------------------------------
# DEPLOYMENT PLATFORM
# ------------------------------------------------------------------------------
# Automatically set by platforms, or specify manually
# Options: vercel, railway, aws, azure, docker, localhost
# DEPLOYMENT_PLATFORM=localhost

# ------------------------------------------------------------------------------
# RPC ENDPOINTS (In priority order - MAINNET)
# ------------------------------------------------------------------------------
# Primary: Use premium RPC providers for production
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com

# Helius (Recommended for production)
# NEXT_PUBLIC_HELIUS_RPC=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# QuickNode (Recommended for production)
# NEXT_PUBLIC_QUICKNODE_RPC=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_KEY/

# Triton (Recommended for production)
# NEXT_PUBLIC_TRITON_RPC=https://your-endpoint.rpcpool.com/YOUR_KEY

# ------------------------------------------------------------------------------
# BACKEND API CONFIGURATION
# ------------------------------------------------------------------------------
# Backend API URL (for server-based deployments)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# WebSocket URL for real-time updates
NEXT_PUBLIC_WS_URL=ws://localhost:3000

# For Docker Compose, use service names:
# NEXT_PUBLIC_BACKEND_URL=http://backend:3000
# NEXT_PUBLIC_WS_URL=ws://backend:3000

# ------------------------------------------------------------------------------
# JUPITER AGGREGATOR
# ------------------------------------------------------------------------------
# Jupiter API endpoint
NEXT_PUBLIC_JUPITER_API_URL=https://quote-api.jup.ag

# Jupiter V6 Program ID
NEXT_PUBLIC_JUPITER_V6_PROGRAM_ID=JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4

# ------------------------------------------------------------------------------
# PYTH NETWORK (Price Feeds)
# ------------------------------------------------------------------------------
# Pyth Network Hermes endpoint
NEXT_PUBLIC_PYTH_HERMES_ENDPOINT=https://hermes.pyth.network
PYTH_HERMES_ENDPOINT=https://hermes.pyth.network

# ------------------------------------------------------------------------------
# WALLET CONFIGURATION
# ------------------------------------------------------------------------------
# WalletConnect Project ID (Get from https://cloud.walletconnect.com/)
# Required for mobile wallet connections
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=YOUR_PROJECT_ID_HERE

# ------------------------------------------------------------------------------
# TRADING CONFIGURATION
# ------------------------------------------------------------------------------
# Minimum profit threshold in SOL
NEXT_PUBLIC_MINIMUM_PROFIT_SOL=0.005

# Maximum slippage tolerance (0.01 = 1%)
NEXT_PUBLIC_MAX_SLIPPAGE=0.01

# Gas buffer multiplier
NEXT_PUBLIC_GAS_BUFFER=1.5

# ------------------------------------------------------------------------------
# SERVER-SIDE ONLY VARIABLES (NOT NEXT_PUBLIC_)
# ------------------------------------------------------------------------------
# These variables are only accessible server-side
# DO NOT prefix with NEXT_PUBLIC_ to keep them secure

# Transaction Signing (Server-side only)
TRANSACTION_SIGNING_KEY=your_key_here

# Neynar API (Server-side only, for Farcaster integration)
NEYNAR_API_KEY=your_key_here

# Database (Server-side only)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=gxq_studio

# ------------------------------------------------------------------------------
# BUILD-TIME VARIABLES (Docker)
# ------------------------------------------------------------------------------
# These are used during Docker build
# Set as ARG in Dockerfile or --build-arg in docker build

# DOCKER=true
# NODE_ENV=production

# ------------------------------------------------------------------------------
# SECURITY NOTES
# ------------------------------------------------------------------------------
# 1. NEXT_PUBLIC_* variables are embedded in the client-side bundle
# 2. Never put private keys or secrets in NEXT_PUBLIC_* variables
# 3. Server-side variables are only accessible in API routes and getServerSideProps
# 4. Use environment-specific .env files (.env.local, .env.production)
# 5. Never commit .env files with real values to version control
# ------------------------------------------------------------------------------
