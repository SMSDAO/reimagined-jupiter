# Dual-Approval Workflow Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    DUAL-APPROVAL DEPLOYMENT WORKFLOW                     │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│  STEP 1:     │
│  PREPARE TX  │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  TransactionBuilder.buildTransaction()                          │
│  - Create instructions                                          │
│  - Set fee payer                                                │
│  - Add compute budget                                           │
│  - Get recent blockhash                                         │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  TransactionBuilder.simulateTransactionAdvanced()               │
│  - Run simulation                                               │
│  - Calculate Value at Risk (VaR)                                │
│  - Extract target Program ID                                    │
│  - Analyze balance changes                                      │
│                                                                  │
│  OUTPUT:                                                         │
│  ✓ Value at Risk: 2.5 SOL                                      │
│  ✓ Target Program: Fg6Pa...FsLnS                               │
│  ✓ Risk Level: MEDIUM                                          │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  TransactionBuilder.serializeUnsignedTransaction()              │
│  - Serialize to Base64                                          │
│  - Extract instruction details                                  │
│  - Store for approval workflow                                  │
│                                                                  │
│  OUTPUT: Base64 serialized transaction (offline signing ready)  │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│  STEP 2:     │
│  REQUEST     │
│  APPROVAL    │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ApprovalService.createApprovalRequest()                        │
│  - Generate transaction hash                                    │
│  - Check for duplicates                                         │
│  - Set expiration (24h default)                                 │
│  - Store in pending_approvals table                             │
│  - Create audit log entry                                       │
│                                                                  │
│  DATABASE: INSERT INTO pending_approvals                        │
│    - transaction_hash: abc123...                                │
│    - serialized_transaction: eyJhb...                           │
│    - value_at_risk: 2.5                                         │
│    - status: PENDING                                            │
│    - requested_by: admin_user                                   │
│    - expires_at: 2025-01-24T08:00:00Z                          │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  UI: Admin Panel (/admin)                                       │
│  - Display in PendingApprovals component                        │
│  - Show risk level badge (color-coded)                          │
│  - Auto-refresh every 30 seconds                                │
│  - Notification badge: "1 transaction awaiting approval"        │
└──────┬──────────────────────────────────────────────────────────┘
       │
       │  ⏳ WAITING FOR SUPER_ADMIN...
       │
       ▼
┌──────────────┐
│  STEP 3:     │
│  SUPER_ADMIN │
│  REVIEW      │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  SUPER_ADMIN clicks "View Details"                              │
│  → SimulationSummary modal appears                              │
│                                                                  │
│  ╔══════════════════════════════════════════════════════════╗  │
│  ║  🔍 Pre-flight Simulation            [MEDIUM RISK]      ║  │
│  ╠══════════════════════════════════════════════════════════╣  │
│  ║  ⚠️  Value at Risk                                      ║  │
│  ║      2.5000 SOL                                          ║  │
│  ║      Maximum potential loss from this transaction        ║  │
│  ║                                                          ║  │
│  ║  🎯 Target Program ID                                   ║  │
│  ║      Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS      ║  │
│  ║      Deployment/upgrade target address                   ║  │
│  ║                                                          ║  │
│  ║  📋 Transaction Type: PROGRAM_DEPLOYMENT                ║  │
│  ║      3 instructions                                      ║  │
│  ║                                                          ║  │
│  ║  🔒 Dual-Approval Security: This transaction requires   ║  │
│  ║     approval from a SUPER_ADMIN before broadcast.       ║  │
│  ╠══════════════════════════════════════════════════════════╣  │
│  ║  [ ❌ Reject ]              [ ✅ Approve ]              ║  │
│  ╚══════════════════════════════════════════════════════════╝  │
└──────┬──────────────────────────────────────────────────────────┘
       │
       │  SUPER_ADMIN clicks "Approve"
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ApprovalService.processApproval()                              │
│  1. Verify SUPER_ADMIN role via RBAC                            │
│  2. Check approval not expired                                  │
│  3. Prevent self-approval (requester ≠ approver) ✓             │
│  4. Update status to APPROVED                                   │
│  5. Record approver details                                     │
│  6. Create audit log entry                                      │
│                                                                  │
│  DATABASE: UPDATE pending_approvals                             │
│    SET status = 'APPROVED',                                     │
│        approved_by = 'super_admin_reviewer',                    │
│        approved_at = NOW(),                                     │
│        approval_reason = 'Security audit passed'                │
│    WHERE id = 'approval-uuid'                                   │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│  STEP 4:     │
│  EXECUTE     │
│  TRANSACTION │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  TransactionBuilder.deserializeTransaction()                    │
│  - Convert Base64 back to Transaction object                    │
│  - Restore blockhash and metadata                               │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  TransactionBuilder.executeTransaction(tx, signers, approvalId) │
│                                                                  │
│  🔒 APPROVAL ENFORCEMENT AT RPC LAYER:                          │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ verifyApproval(approvalId, transaction)                  │  │
│  │ 1. Query pending_approvals by ID                         │  │
│  │ 2. Verify status = 'APPROVED' ✓                         │  │
│  │ 3. Check not expired ✓                                  │  │
│  │ 4. Verify transaction hash matches ✓                    │  │
│  │ 5. Confirm approver has SUPER_ADMIN role ✓             │  │
│  │                                                          │  │
│  │ IF ANY CHECK FAILS:                                      │  │
│  │   return { success: false, error: 'Approval required' }  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ✓ Approval verified, proceeding with execution...              │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  Sign and broadcast to Solana network                           │
│  - Sign with keypairs                                           │
│  - Send via RPC                                                 │
│  - Confirm transaction                                          │
│  - Retrieve signature and metrics                               │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ApprovalService.markAsExecuted()                               │
│  - Update status to EXECUTED                                    │
│  - Record transaction signature                                 │
│  - Set execution timestamp                                      │
│  - Create audit log entry                                       │
│                                                                  │
│  DATABASE: UPDATE pending_approvals                             │
│    SET status = 'EXECUTED',                                     │
│        executed = true,                                         │
│        execution_signature = '5Kx3m...',                        │
│        executed_at = NOW()                                      │
│    WHERE id = 'approval-uuid'                                   │
└──────┬──────────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│  ✅ DEPLOYMENT COMPLETE                                         │
│                                                                  │
│  Signature: 5Kx3m7qR8Fv...                                      │
│  Compute Units: 142,573                                         │
│  Fee: 0.000005 SOL                                              │
│  Status: Confirmed                                              │
│                                                                  │
│  AUDIT TRAIL CREATED:                                           │
│  1. Approval request created by admin_user                      │
│  2. Transaction simulated (VaR: 2.5 SOL)                       │
│  3. Approved by super_admin_reviewer                            │
│  4. Executed successfully (signature: 5Kx3m...)                │
└─────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════
                        SECURITY CHECKPOINTS
═══════════════════════════════════════════════════════════════════

✓ Checkpoint 1: Transaction Preparation
  - Simulation calculates Value at Risk
  - Target program ID extracted
  - Risk level assessed

✓ Checkpoint 2: Approval Request
  - Transaction hash generated for integrity
  - Duplicate detection prevents replay
  - Expiration timestamp set

✓ Checkpoint 3: SUPER_ADMIN Review
  - Role verification via RBAC
  - Self-approval prevention enforced
  - Complete simulation data displayed

✓ Checkpoint 4: RPC-Layer Enforcement
  - Approval status verified
  - Transaction hash validated
  - Cannot bypass via alternative paths

✓ Checkpoint 5: Audit Trail
  - All actions logged with timestamps
  - User IDs and usernames recorded
  - Complete history maintained

═══════════════════════════════════════════════════════════════════
                        FAILURE SCENARIOS
═══════════════════════════════════════════════════════════════════

❌ NO APPROVAL:
   executeTransaction() → verifyApproval() → return error
   Result: Transaction blocked

❌ EXPIRED APPROVAL:
   verifyApproval() checks expires_at < NOW() → return false
   Result: Transaction blocked, new approval required

❌ SELF-APPROVAL ATTEMPT:
   processApproval() checks requester === approver → throw error
   Result: Approval denied

❌ INSUFFICIENT PERMISSIONS:
   processApproval() checks RBAC → hasPermission() → false
   Result: Permission denied

❌ TRANSACTION TAMPERING:
   verifyApproval() compares transaction hash → mismatch → return false
   Result: Transaction blocked, integrity violation logged

═══════════════════════════════════════════════════════════════════
```
