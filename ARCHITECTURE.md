# 🏗️ GXQ Studio - System Architecture

## Overview

GXQ Studio is a production-ready Solana trading bot system with multiple deployment options, comprehensive monitoring, and enterprise-grade security.

## 📊 High-Level Architecture

```
┌────────────────────────────────────────────────────────────────┐
│                         USER LAYER                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  Trader  │  │  Admin   │  │   CLI    │  │   API    │      │
│  │  (Web)   │  │ (Panel)  │  │  Tools   │  │  Client  │      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘      │
└───────┼─────────────┼─────────────┼─────────────┼─────────────┘
        │             │             │             │
        └─────────────┴─────────────┴─────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                      FRONTEND LAYER                             │
│  Next.js 16 App Router + React 19 + Tailwind CSS 4            │
│                                                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  /swap   │  │  /admin  │  │/arbitrage│  │/launchpad│     │
│  │  (Trade) │  │ (Control)│  │ (Scan)   │  │ (Launch) │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │ /sniper  │  │ /staking │  │/airdrop  │  │/analysis │     │
│  │ (Bot)    │  │ (Stake)  │  │ (Check)  │  │ (Wallet) │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
│                                                                │
│  Wallet Adapters: Phantom, Solflare, Backpack, Glow, etc.    │
└────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                      API LAYER                                  │
│  Serverless Functions (Vercel) or Express (Railway)           │
│                                                                │
│  PUBLIC ENDPOINTS:                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                    │
│  │ /monitor │  │ /execute │  │  /health │                    │
│  │  (cron)  │  │  (cron)  │  │  (GET)   │                    │
│  └──────────┘  └──────────┘  └──────────┘                    │
│                                                                │
│  ADMIN ENDPOINTS (JWT Required):                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  /auth   │  │ /control │  │ /metrics │  │  /logs   │     │
│  │  (POST)  │  │  (POST)  │  │  (GET)   │  │  (GET)   │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
└────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                   CORE LIBRARY LAYER                            │
│  Business Logic & Trading Engine                               │
│                                                                │
│  ┌────────────────────────────────────────────────────┐       │
│  │  SCANNER  (lib/scanner.ts)                         │       │
│  │  • Parallel token pair scanning                     │       │
│  │  • Multi-strategy detection                         │       │
│  │  • Jupiter price comparison                         │       │
│  │  • Profit calculation with fees                     │       │
│  │  • Confidence scoring                               │       │
│  └────────────────────────────────────────────────────┘       │
│                                                                │
│  ┌────────────────────────────────────────────────────┐       │
│  │  EXECUTOR  (lib/executor.ts)                       │       │
│  │  • Jupiter swap building                            │       │
│  │  • Dynamic priority fees                            │       │
│  │  • Transaction signing                              │       │
│  │  • Confirmation tracking                            │       │
│  │  • Retry with exponential backoff                   │       │
│  └────────────────────────────────────────────────────┘       │
│                                                                │
│  ┌────────────────────────────────────────────────────┐       │
│  │  LOGGER  (lib/logger.ts)                           │       │
│  │  • Structured JSON logging                          │       │
│  │  • Multiple log levels                              │       │
│  │  • Request ID tracking                              │       │
│  │  • Performance timing                               │       │
│  │  • Color-coded console (dev)                        │       │
│  └────────────────────────────────────────────────────┘       │
│                                                                │
│  ┌────────────────────────────────────────────────────┐       │
│  │  AUTH  (lib/auth.ts)                               │       │
│  │  • JWT generation/verification                      │       │
│  │  • Bcrypt password hashing                          │       │
│  │  • Rate limiting                                    │       │
│  │  • Token extraction                                 │       │
│  │  • API key management                               │       │
│  └────────────────────────────────────────────────────┘       │
└────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────────────────────┐
│                  BLOCKCHAIN LAYER                               │
│  Solana Mainnet Integration                                    │
│                                                                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │  Solana  │  │ Jupiter  │  │  Flash   │  │   DEXs   │     │
│  │   RPC    │  │  API v6  │  │  Loans   │  │  (Multi) │     │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘     │
│       │              │              │              │          │
│       │              │              │              │          │
│  ┌────┴────┐   ┌────┴────┐   ┌────┴────┐   ┌────┴────┐     │
│  │ Balance │   │  Quote  │   │Marginfi │   │Raydium  │     │
│  │Transaction   │  Swap   │   │ Solend  │   │  Orca   │     │
│  │ Confirm │   │  Route  │   │  Mango  │   │Meteora  │     │
│  └─────────┘   └─────────┘   │ Kamino  │   │Phoenix  │     │
│                               │  Port   │   │ Pump.fun│     │
│                               └─────────┘   └─────────┘     │
└────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### 1. Opportunity Discovery Flow

```
┌─────────┐
│  CRON   │ (Vercel: Every 1 min)
│ TRIGGER │ (Railway: Continuous)
└────┬────┘
     │
     ▼
┌──────────────────┐
│  /api/monitor    │
│  • Auth check    │
│  • RPC connect   │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  lib/scanner     │
│  • Fetch pairs   │
│  • Get quotes    │
│  • Calculate     │
│  • Filter        │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  Jupiter API     │
│  • Quote request │
│  • Route plan    │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  Opportunities   │
│  • Stored/cached │
│  • Logged        │
└──────────────────┘
```

### 2. Trade Execution Flow

```
┌─────────┐
│  CRON   │ (Vercel: Every 5 min)
│ TRIGGER │ (Railway: Immediate)
└────┬────┘
     │
     ▼
┌──────────────────┐
│  /api/execute    │
│  • Auth check    │
│  • Load wallet   │
│  • Check balance │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│ Validate Opps    │
│  • Re-check      │
│  • Still valid?  │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  lib/executor    │
│  • Build tx      │
│  • Sign tx       │
│  • Send tx       │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  Jupiter Swap    │
│  • Quote         │
│  • Transaction   │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  Solana Network  │
│  • Broadcast     │
│  • Confirm       │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  Result          │
│  • Log trade     │
│  • Update metrics│
│  • Record profit │
└──────────────────┘
```

### 3. Admin Control Flow

```
┌─────────┐
│  ADMIN  │
│  PANEL  │
└────┬────┘
     │
     ▼
┌──────────────────┐
│ /api/admin/auth  │
│  • Credentials   │
│  • Rate limit    │
│  • Hash check    │
└────┬─────────────┘
     │
     ▼
┌──────────────────┐
│  JWT Token       │
│  • 24h validity  │
│  • Stored client │
└────┬─────────────┘
     │
     ▼
┌──────────────────────────────────────────┐
│         Authenticated Actions             │
│  ┌────────────┐  ┌──────────────────┐   │
│  │  /control  │  │    /metrics      │   │
│  │  start/stop│  │ profit/trades    │   │
│  └────────────┘  └──────────────────┘   │
│  ┌────────────┐  ┌──────────────────┐   │
│  │   /logs    │  │   /opportunities │   │
│  │  view/export   │ live feed        │   │
│  └────────────┘  └──────────────────┘   │
└──────────────────────────────────────────┘
```

## 🌐 Deployment Architectures

### Vercel Architecture (Serverless)

```
┌──────────────────────────────────────────────┐
│           Vercel Edge Network                 │
│  ┌────────────┐  ┌────────────┐             │
│  │    CDN     │  │   HTTPS    │             │
│  └────────────┘  └────────────┘             │
└────────────┬─────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│         Serverless Functions                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │ monitor  │  │ execute  │  │  health  │    │
│  │ (60s)    │  │  (60s)   │  │  (10s)   │    │
│  └──────────┘  └──────────┘  └──────────┘    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │   auth   │  │ control  │  │ metrics  │    │
│  │  (10s)   │  │  (10s)   │  │  (30s)   │    │
│  └──────────┘  └──────────┘  └──────────┘    │
└────────────────────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│          Vercel Cron Jobs                      │
│  • /api/monitor - Every 1 minute              │
│  • /api/execute - Every 5 minutes             │
└────────────────────────────────────────────────┘
```

### Railway Architecture (24/7)

```
┌──────────────────────────────────────────────┐
│           Railway Service                     │
│  ┌────────────┐  ┌────────────┐             │
│  │ Load Balancer  │  HTTPS    │             │
│  └────────────┘  └────────────┘             │
└────────────┬─────────────────────────────────┘
             │
             ▼
┌────────────────────────────────────────────────┐
│         Express.js Server                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │  /health │  │ /metrics │  │ /control │    │
│  │  (GET)   │  │  (GET)   │  │  (POST)  │    │
│  └──────────┘  └──────────┘  └──────────┘    │
│                                                │
│  ┌────────────────────────────────────────┐  │
│  │   Background Monitoring Loop            │  │
│  │   • Continuous (5-second cycle)         │  │
│  │   • Scan → Execute → Repeat             │  │
│  └────────────────────────────────────────┘  │
│                                                │
│  ┌────────────────────────────────────────┐  │
│  │   Auto-Restart on Failure               │  │
│  │   • Max 10 retries                      │  │
│  │   • Graceful shutdown (SIGTERM)         │  │
│  └────────────────────────────────────────┘  │
└────────────────────────────────────────────────┘
```

## 🔐 Security Architecture

```
┌──────────────────────────────────────────────┐
│          Security Layers                      │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  NETWORK SECURITY                       │ │
│  │  • HTTPS only                           │ │
│  │  • CORS configuration                   │ │
│  │  • DDoS protection (Vercel/Railway)    │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  AUTHENTICATION                         │ │
│  │  • JWT tokens (24h expiry)             │ │
│  │  • Bcrypt hashing (10 rounds)          │ │
│  │  • Rate limiting (5/15min)             │ │
│  │  • Cron authorization                   │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  SECRETS MANAGEMENT                     │ │
│  │  • Environment variables                │ │
│  │  • No hardcoded keys                    │ │
│  │  • .env isolation                       │ │
│  │  • No logs of secrets                   │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  WALLET SECURITY                        │ │
│  │  • Base58 private key                   │ │
│  │  • Balance checks                       │ │
│  │  • Transaction confirmation             │ │
│  │  • Dedicated trading wallet             │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

## 📊 Monitoring Architecture

```
┌──────────────────────────────────────────────┐
│         Monitoring & Observability            │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  HEALTH MONITORING                      │ │
│  │  • RPC latency tracking                 │ │
│  │  • Wallet balance monitoring            │ │
│  │  • API availability checks              │ │
│  │  • Error rate calculation               │ │
│  │  • Uptime tracking                      │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  PERFORMANCE METRICS                    │ │
│  │  • Scan frequency/duration              │ │
│  │  • Trade execution time                 │ │
│  │  • Success/failure rates                │ │
│  │  • Gas usage tracking                   │ │
│  │  • Profit accumulation                  │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  LOGGING                                │ │
│  │  • Structured JSON logs                 │ │
│  │  • Multiple severity levels             │ │
│  │  • Request ID tracking                  │ │
│  │  • Performance timing                   │ │
│  │  • Trade audit trail                    │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  ALERTING (Future)                      │ │
│  │  • Low balance warnings                 │ │
│  │  • High error rate alerts               │ │
│  │  • RPC failure notifications            │ │
│  │  • Unusual activity detection           │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

## 🎯 Trading Strategy Architecture

```
┌──────────────────────────────────────────────┐
│         Trading Strategies                    │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  ARBITRAGE                              │ │
│  │  • Cross-DEX price differences          │ │
│  │  • Buy low, sell high                   │ │
│  │  • Small capital, frequent trades       │ │
│  │  • 0.3-1% profit target                 │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  FLASH LOAN ARBITRAGE                   │ │
│  │  • Borrow large amount (no collateral)  │ │
│  │  • Execute arbitrage trade              │ │
│  │  • Repay loan + fee (0.09%)            │ │
│  │  • Keep profit                          │ │
│  └────────────────────────────────────────┘ │
│                                               │
│  ┌────────────────────────────────────────┐ │
│  │  TRIANGULAR ARBITRAGE                   │ │
│  │  • Three-way currency exchange          │ │
│  │  • SOL → USDC → USDT → SOL            │ │
│  │  • Exploit price inefficiencies         │ │
│  │  • Higher complexity, higher reward     │ │
│  └────────────────────────────────────────┘ │
└──────────────────────────────────────────────┘
```

## 🔧 Technology Stack

### Frontend:
- **Framework:** Next.js 16 (App Router)
- **UI Library:** React 19
- **Styling:** Tailwind CSS 4
- **3D Graphics:** Three.js, React Three Fiber
- **Animation:** Framer Motion
- **Wallet:** @solana/wallet-adapter-react

### Backend:
- **Runtime:** Node.js 18
- **Language:** TypeScript (ES2022)
- **Blockchain:** @solana/web3.js
- **DEX:** @jup-ag/api (Jupiter v6)
- **Server:** Express.js (Railway mode)
- **Auth:** jsonwebtoken, bcrypt

### Infrastructure:
- **Serverless:** Vercel Functions
- **Continuous:** Railway
- **Container:** Docker (Alpine Linux)
- **Build:** Nixpacks / Dockerfile

### Monitoring:
- **Logging:** Custom structured logger
- **Metrics:** In-memory tracking
- **Health:** HTTP endpoints

## 📈 Scaling Considerations

### Horizontal Scaling:
- Multiple bot instances per user
- Load balancing via Railway/cloud
- Distributed caching (future: Redis)
- Database for persistent state (future: PostgreSQL)

### Vertical Scaling:
- Increase RPC rate limits
- Premium RPC providers
- Larger trade sizes
- More frequent scans

### Performance Optimization:
- Connection pooling
- Request caching
- Parallel processing
- Efficient algorithms

## 📝 Logging & Monitoring

### Centralized Logging System

GXQ Studio uses **Winston** for centralized, structured logging with the following features:

**Log Levels:**
- `error`: Critical errors requiring immediate attention
- `warn`: Warning conditions that should be reviewed
- `info`: Informational messages about normal operations
- `debug`: Detailed debugging information
- `verbose`: Very detailed trace information

**Log Features:**
- **Request ID Tracking**: Each request gets a unique ID for tracing
- **Context-Aware Logging**: Logs include context (e.g., "RPC", "Transaction", "Auth")
- **Structured Metadata**: JSON-formatted additional information
- **Log Rotation**: Automatic rotation at 5MB (5 error files, 10 combined files)
- **Multiple Transports**: Console (colorized) + File output

**Specialized Logging Methods:**
```typescript
import { Logger } from './utils/logger';

const logger = new Logger('MyService');

// RPC errors
logger.rpcError('getBalance', error, { address: '...' });

// Transaction failures
logger.transactionError(signature, error, { amount, token });

// Authentication events
logger.authEvent('login', true, { username });

// Arbitrage opportunities
logger.opportunity(profitAmount, ['DEX1', 'DEX2'], { slippage });

// Trade execution
logger.trade('flash-loan-arbitrage', true, { profit, signature });
```

**Log Files:**
- `logs/error.log` - Error-level messages only
- `logs/combined.log` - All log levels

**Environment Variable:**
- `LOG_LEVEL` - Set log level (default: 'info')

## 🔧 TypeScript Configuration

### Strict Mode Enforcement

The project enforces **TypeScript strict mode** for maximum type safety:

```json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "types": ["node", "jest"]
  }
}
```

**Type Definitions:**
- All external modules have proper `@types/*` packages installed
- Custom types defined in `src/types.ts`
- Test files excluded from build (use separate tsconfig for tests)

**ESLint Rules:**
- `@typescript-eslint/no-explicit-any`: warn (not error)
- `@typescript-eslint/no-unused-vars`: error (with `argsIgnorePattern: "^_"`)

---

**For detailed deployment instructions, see:**
- `QUICKSTART.md` - 5-minute setup
- `docs/archive/DEPLOYMENT.md` - Complete guide (archived)
